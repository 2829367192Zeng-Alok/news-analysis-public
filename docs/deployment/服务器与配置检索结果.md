# 服务器与配置检索结果

基于项目计划、工作区与 SSH 连接 ECS 后的检索结果整理。

---

## 一、项目计划概要

- **名称**：金融新闻分析系统  
- **目标**：新闻采集（Tushare）→ AI 筛选/详细分析（豆包）→ 云数据库 → Web 展示，部署于阿里云 ECS。  
- **计划文件**：`.cursor/plans/金融新闻分析系统_d1f1a06f.plan.md`  
- **当前状态**：采集、筛选、分析、定时任务、Flask 后端与前端均已完成（计划内 todo 均为 completed）。

---

## 二、工作区结构（本地）

```
financial-news-analysis/
├── app.py
├── config.py
├── models.py
├── news_fetcher.py, news_filter.py, news_analyzer.py
├── run_task.py, run_pipeline_once.py, run_analysis_latest_20.py
├── doubao_client.py, prompts.py, news_sources.py, utils.py
├── init_db.py, check_db.py
├── requirements.txt, README.md, .env.example
├── templates/index.html
├── static/css/style.css, static/js/main.js
├── web_display/           # 静态展示（Tabler）
│   ├── index.html
│   └── data/.gitkeep
├── sync_news_to_display.py # 同步 news_analysis_detail → web_display/data/feed.json
├── scripts/setup_web_display_on_server.sh
├── docs/
│   ├── 连接服务器与操作数据库.md
│   ├── 部署Web展示系统.md
│   ├── 上传并部署到ECS.md
│   └── 服务器与配置检索结果.md（本文件）
└── tests/
```

---

## 三、SSH 配置（本机默认）

**文件位置**：`C:\Users\28293\.ssh\config`

| 项     | 值 |
|--------|----|
| Host   | `aliyun` |
| HostName | `47.110.3.177` |
| User   | `root` |
| Port   | `22` |
| IdentityFile | `C:/Users/28293/.ssh/id_ed25519` |

**连接命令**：`ssh aliyun`

---

## 四、数据库 / 项目配置的默认位置

来自 `config.py` 的加载逻辑：

1. **配置文件加载顺序**（需安装 `python-dotenv`）：  
   - 先：**用户目录** `~/.financial_news_analysis.env`  
   - 后：**项目当前工作目录** `./.env`（后者覆盖前者）

2. **本机**  
   - 项目目录：`C:\Users\28293\Desktop\金融\代码\cursor1\financial-news-analysis`  
   - 默认配置路径：项目下 `.env` 或用户目录 `C:\Users\28293\.financial_news_analysis.env`

3. **ECS 上**（检索结果）  
   - 项目目录：`/root/workspace/project/financial-news-analysis`  
   - 实际使用：**项目目录下的 `.env` 已存在**  
   - 用户目录下未发现 `/root/.financial_news_analysis.env`  
   - 因此运行时以 **项目内 `.env`** 为准（DB_HOST 等应在该文件中配置，ECS 上建议为 RDS 内网地址）。

---

## 五、ECS 上现有代码检索结果（2025-03-03）

**项目路径**：`/root/workspace/project/financial-news-analysis`

**已有文件/目录**：

| 类型     | 路径/文件 |
|----------|-----------|
| 主程序   | app.py, config.py, models.py |
| 采集/分析 | news_fetcher.py, news_analyzer.py, news_filter.py, doubao_client.py, prompts.py, news_sources.py, utils.py |
| 任务/工具 | run_task.py, run_pipeline_once.py, run_analysis_latest_20.py, init_db.py |
| 调试/测试 | debug_filter_response.py, debug_news_api.py, test_filter_once.py |
| 配置     | .env（存在） |
| Web      | static/css, static/js, templates/index.html |
| 其他     | README.md, requirements.txt, analysis_report.txt, pipeline_report.txt, __pycache__ |

**服务器上当前没有的**（本地已有）：

- `web_display/` 目录（含 index.html、data）
- `sync_news_to_display.py`
- `scripts/setup_web_display_on_server.sh`
- `docs/` 下多份文档（如部署、上传说明）

如需在 ECS 上使用静态展示与同步脚本，需将上述文件上传到服务器，并按要求执行（参见 `docs/上传并部署到ECS.md`）。

---

## 六、小结

- **项目计划**：见 `.cursor/plans/金融新闻分析系统_d1f1a06f.plan.md`，各模块已完成。  
- **工作区**：本地包含完整代码及 `web_display`、同步脚本与文档。  
- **SSH**：本机使用 `ssh aliyun`，对应 `47.110.3.177`，root + 密钥。  
- **配置默认位置**：项目目录 `.env` 优先；ECS 上仅存在项目内 `.env`，数据库等通过该文件（含内网地址）连接。  
- **ECS 现状**：主流程与 Flask 前端已部署；静态展示与同步脚本需按文档上传并部署。
