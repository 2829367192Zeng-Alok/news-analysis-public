.env
logs/

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.pytest_cache/
.mypy_cache/
*.egg-info/
.eggs/
dist/
build/

# Runtime CSV exports (ECS / local pipelines regenerate from APIs & DB)
news/*.csv

# Generated reports (pipeline / analysis runs)
analysis_report.txt
pipeline_report.txt

# Alert suppression state (runtime, regenerated)
.alert_state.json

# Server-local TLS/cert material (ECS 上的证书私钥等，严禁入库——仓库含公开远端)
web_display/cert_tmp/
web_display/credit.zip

# Web display runtime output (synced from DB)
web_display/data/*.json
web_display/.sync_state.json

# go-cqhttp 二进制与运行时数据
go-cqhttp/go-cqhttp
go-cqhttp/go-cqhttp.exe
go-cqhttp/*.bat
go-cqhttp/data/
go-cqhttp/logs/
go-cqhttp/session.token

# 调试与临时脚本（移入 scripts/ 目录后也不纳入版本控制）
scripts/debug_*.py
scripts/test_*.py
scripts/fix_*.py

# Runtime CSV exports (ECS / local pipelines regenerate from APIs & DB)
news/*.csv
